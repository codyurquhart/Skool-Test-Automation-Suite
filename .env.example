# Copy to `.env` and fill in values for local runs.
# Playwright loads this via dotenv in playwright.config.js

# CUSTOMIZE: Application under test (include scheme, no trailing slash required by Playwright)
BASE_URL=https://example.com

PERCY_TOKEN=
